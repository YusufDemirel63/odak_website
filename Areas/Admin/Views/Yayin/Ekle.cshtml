@model Yayin
@{ViewData["Title"] = Model.Id == 0 ? "Yeni Yayin" : "Yayin Duzenle"; var isEdit = Model.Id != 0;}
<div class="admin-card">
    <div class="admin-card-header">
        <h3><i class="fas fa-book" style="color:var(--admin-accent);"></i> @ViewData["Title"]</h3>
        <a asp-action="Index" class="btn-admin btn-admin-edit"><i class="fas fa-arrow-left"></i> Geri</a>
    </div>
    <form asp-action="@(isEdit ? "Duzenle" : "Ekle")" method="post" enctype="multipart/form-data" class="admin-form">
        @Html.AntiForgeryToken()
        @if (isEdit) { <input type="hidden" asp-for="Id"> <input type="hidden" asp-for="OlusturulmaTarihi"> }
        <div style="display:grid; grid-template-columns:repeat(auto-fit, minmax(280px, 1fr)); gap:20px;">
            <div class="form-group"><label>Baslik *</label><input asp-for="Baslik"></div>
            <div class="form-group"><label>Iayin Turu *</label>
                <select asp-for="YayinTuru"><option value="">Secin</option><option value="Kitap">Kitap</option><option value="Rapor">Rapor</option><option value="Makale">Makale</option><option value="Bulten">Bulten</option></select>
            </div>
            <div class="form-group"><label>Yayin Tarihi</label><input asp-for="YayinTarihi" type="date"></div>
            <div class="form-group"><label>Dis Link</label><input asp-for="DisLink" type="url" placeholder="https://..."></div>
        </div>
        <div class="form-group"><label>Aciklama</label><textarea asp-for="Aciklama"></textarea></div>
        <div class="form-group"><label>Gorsel</label><input type="file" name="gorselFile" accept="image/*">
            @if (!string.IsNullOrEmpty(Model.GorselYolu)) { <img src="@Model.GorselYolu" style="height:60px;margin-top:8px;border-radius:4px;"> <input type="hidden" asp-for="GorselYolu"> }
        </div>
        <div class="form-group"><label>PDF Dosyasi</label><input type="file" name="dosyaFile" accept=".pdf">
            @if (!string.IsNullOrEmpty(Model.DosyaYolu)) { <p style="margin-top:8px;color:#28a745;font-size:0.85rem;"><i class="fas fa-file-pdf"></i> Mevcut PDF var</p> <input type="hidden" asp-for="DosyaYolu"> }
        </div>
        <div class="form-group" style="display:flex;align-items:center;gap:10px;"><input asp-for="Aktif" type="checkbox" style="width:auto;margin:0;"><label asp-for="Aktif" style="margin:0;">Aktif</label></div>
        <button type="submit" class="btn-admin btn-admin-primary" style="padding:12px 30px;font-size:1rem;"><i class="fas fa-save"></i> @(isEdit ? "Guncelle" : "Kaydet")</button>
    </form>
</div>
